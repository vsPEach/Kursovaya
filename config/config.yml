logger:
  level: 0
  encoding: "console"
  output:
    - "stdout"
    - "logger_logs.log"
rpcserver:
  host: "localhost"
  port: 54234
httpserver:
  host: "localhost"
  port: 55238
database:
  implementation: "sql"
  type: "postgres"
  dsn: "host=localhost port=5432 user=postgres password=postgres dbname=calendar sslmode=disable"